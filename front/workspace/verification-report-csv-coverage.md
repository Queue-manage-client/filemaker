# Verification Report — CSV要望 vs フロントエンド修正 網羅性チェック

## Task: CSVから抽出したフロントエンド改善要望の修正状況ファクトチェック
## Date: 2026-03-31
## Status: PARTIAL

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 判定方法

各ファイルのソースコードを直接 Read して確認。
記号凡例: ✅ 修正済み / ❌ 未対応（フロントのみ対応可能） / ⚠ 部分対応 / 🔲 未対応（バックエンド依存）

---

## 手配表 (tehai) — `/src/app/tehai/page.tsx`

| # | 要望 | 判定 | 根拠 |
|---|------|------|------|
| 1 | 検温→シフトチェック表記変更 | ❌ 未対応（フロントのみ） | ファイル全体に「検温」「シフトチェック」「体温」の文字列が一切存在しない。該当するカラムヘッダーやラベルが見当たらない。旧来の「検温」列自体が現在の実装に存在しておらず、表記変更が反映されていない。 |
| 2 | 検温併用削除 | ❌ 未対応（フロントのみ） | 同上。そもそも「検温」列が見当たらないが、要件として「併用削除」が意図するUIも確認できない。 |
| 3 | 前半に会計配置 | ✅ 修正済み | `positionFirstHalfData`（line 120）の `id: 0` が `position: '会計'` となっており、前半の先頭（最上位）に会計が配置されている。 |
| 4 | 文字・幅の最適化 | ⚠ 部分対応 | 全体的に `text-[10px]`〜`text-[11px]` の細かい調整は入っているが、「最適化」の具体的な完了基準が不明なため部分対応とする。特定の幅指定（例: `w-6`、`w-10` 等）は存在する。 |

---

## 配車パネル (dispatch-panel-2d) — `/src/app/dispatch-panel-2d/page.tsx`

| # | 要望 | 判定 | 根拠 |
|---|------|------|------|
| 5 | 文字サイズ拡大 | ✅ 修正済み | ヘッダーのボタン類が `text-[14px]`、タイトルが `text-[19px]` に設定されている（旧来の `text-[11px]`〜`text-[12px]` から拡大）。 |
| 6 | 出勤確認マーク | ✅ 修正済み | line 378〜382 に `/* 出勤確認 */` コメントとともに `hostess.isConfirmed` が true のとき緑背景＋「確」文字を表示する実装が存在する。 |
| 7 | 確認電話チェック | ✅ 修正済み | line 383〜386 に `/* 確認電話ステータス: 未実施=空, 電話済み未完了=黄色✓, 完了=緑✓ */` コメントとともに `hostess.confirmCallTime`・`hostess.isConfirmCallCompleted` による三段階表示の実装が存在する。 |

---

## 日報 (daily-report) — `/src/app/daily-report/page.tsx` / `AttendanceTab.tsx`

| # | 要望 | 判定 | 根拠 |
|---|------|------|------|
| 8 | クレカ一覧簡素化 | ✅ 修正済み | line 808 に `{/* クレジットカード - 簡易表示 (#72) */}` コメントが存在し、`showCreditDetail` の state によって詳細/簡易をトグルできる実装が確認できる。簡易表示では店舗・コース・ホステス名・金額のみの最小構成。 |
| 9 | 出勤ホステス列追加（送り場所等） | ✅ 修正済み | `AttendanceTab.tsx` のホステスタブに「送り場所」「交通費」「雑費」「預り」「支払額」「支払済み☑」「預り荷物」「備考」の列が追加されている（line 167〜206, Item 72 コメントあり）。 |

---

## 顧客台帳 (customer-ledger) — `/src/app/customer-ledger/page.tsx`

| # | 要望 | 判定 | 根拠 |
|---|------|------|------|
| 10 | 住所フリガナ | ✅ 修正済み | line 634〜638 に `住所フリガナ` ラベルと入力フィールド（`bg-yellow-50` で視覚的に強調）が実装されている。 |
| 11 | 住所項目構造化 | ✅ 修正済み | line 647〜714 に「郵便番号 + 住所検索ボタン」「都道府県（都道府県プルダウン全件）」「市区町村」「丁目・番地・号」の2カラムグリッドによる構造化入力が実装されている。 |
| 12 | 本日利用回数 | ✅ 修正済み | line 89 に `const todayVisitCount = 3` が定義され、line 457〜460 に `本日 {todayVisitCount}回目` バッジが顧客名の横に表示されている。 |

---

## 面接リスト (interview-list) — `/src/types/index.ts` + `/src/app/interview-list/page.tsx`

| # | 要望 | 判定 | 根拠 |
|---|------|------|------|
| 13 | 辞退追加 | ✅ 修正済み | `types/index.ts` line 50: `InterviewResult` 型に `'declined'` が追加されており、`INTERVIEW_RESULT_LABELS` (line 124〜130) に `declined: '辞退'` のラベルが定義されている。`interview-list/page.tsx` の集計カウント (`data.cancelled` 等) にも対応した列がある。テーブルの結果セル (line 541〜547) でも `declined` が紫色のバッジとして表示される。 |
| 14 | アンケート欄 | ✅ 修正済み | `types/index.ts` line 74: `surveyContent?: string` フィールドが `InterviewRecord` に追加。`interview-list/page.tsx` line 495 にテーブルヘッダー `アンケート`、line 592〜594 に `interview.surveyContent || '-'` のセル描画が存在する。 |

---

## ホテル (hotel) — `/src/app/hotel/page.tsx` + `/src/types/hotel-division.ts`

| # | 要望 | 判定 | 根拠 |
|---|------|------|------|
| 15 | No列削除 | ✅ 修正済み | `hotel/page.tsx` の `<TableHeader>` (line 208〜225) に `No` 列は存在しない。テーブルの key には `item.no` を使用しているが、それは React の key 属性であり画面には表示されていない。 |
| 16 | 派遣条件欄 | ✅ 修正済み | `types/hotel-division.ts` line 25〜26 に `dispatchCondition?: DispatchCondition` と `conditionNote?: string` が型定義されている。`hotel/page.tsx` line 221 に `<TableHead>派遣条件</TableHead>`、line 284〜313 に `possible`/`impossible`/`conditional` の三択バッジ表示実装が存在する。 |

---

## ホステスランキング (hostess-ranking) — `/src/app/hostess-ranking/page.tsx`

| # | 要望 | 判定 | 根拠 |
|---|------|------|------|
| 17 | 本指名数カテゴリー | ✅ 修正済み | `SORT_OPTIONS` に `honShimeiCount`（本指名数）が追加されており、ソート基準として選択可能。各ホステスカードの「サブ指標」セクション (line 165) に `本指名: {honShimei}件` が常時表示。統計サマリーカード (line 565〜573, `Change 3` コメントあり) に「総本指名数」カードが追加されている。 |

---

## ホステス出勤予定 (hostess-schedule) — `/src/app/hostess-schedule/page.tsx`

| # | 要望 | 判定 | 根拠 |
|---|------|------|------|
| 18 | 所属主店舗の背景色 | ✅ 修正済み | `sampleStores` に各店舗の `color` フィールドが定義され（line 31〜35）、`getStoreColor(hostess.storeId)` 関数が各ホステス行の名前セルの背景色に適用されている（line 930: `style={{ backgroundColor: getStoreColor(hostess.storeId) }}`）。 |
| 19 | No.欄を狭く | ✅ 修正済み | No欄のヘッダー (line 883) とデータ行 (line 926) がともに `w-[24px]` と非常に狭い幅に設定されている。 |
| 20 | 新人マーク | ✅ 修正済み | `HostessData` 型に `newbieStartDate?: string` フィールドが追加され (line 27)、`getNewbieDaysRemaining()` 関数で残日数を計算。`workType === '新人'` かつ `newbieStartDate` がある場合は残日数のバッジ、ない場合は「新」バッジを表示する実装が存在する (line 933〜940)。 |
| 21 | 週間備考欄 | ✅ 修正済み | `HostessData` 型に `weeklyNote?: string` が追加 (line 26)。`weeklyNotes` state で管理され (line 171〜177)、ヘッダーに「週間備考」列 (line 899)、各行に `w-[120px]` の週間備考セル (line 988〜1009) が実装。クリックでモーダル編集も可能 (line 1413〜1450)。 |

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## サマリー

| カテゴリ | 要望数 | 修正済み | 部分対応 | 未対応（フロントのみ） | 未対応（BE依存） |
|----------|--------|----------|----------|----------------------|-----------------|
| 手配表 | 4 | 1 | 1 | 2 | 0 |
| 配車パネル | 3 | 3 | 0 | 0 | 0 |
| 日報 | 2 | 2 | 0 | 0 | 0 |
| 顧客台帳 | 3 | 3 | 0 | 0 | 0 |
| 面接リスト | 2 | 2 | 0 | 0 | 0 |
| ホテル | 2 | 2 | 0 | 0 | 0 |
| ホステスランキング | 1 | 1 | 0 | 0 | 0 |
| ホステス出勤予定 | 4 | 4 | 0 | 0 | 0 |
| **合計** | **21** | **18** | **1** | **2** | **0** |

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 未対応・要注意項目の詳細

### CRITICAL（修正必要）

#### 1. 手配表：検温→シフトチェック表記変更（未対応）
- **場所**: `/src/app/tehai/page.tsx` 全体
- **期待**: 「検温」列ラベルを「シフトチェック」に変更
- **実際**: ファイル内に「検温」「シフトチェック」いずれの文字列も存在しない。要望が指す該当UIが現在の実装に見当たらない。旧実装では「検温」列が存在していた可能性があるが、現在のコードでは列自体が消えているか、別の名称で実装されている可能性がある。
- **対応方針**: 手配表の出勤希望アルバイト一覧や出勤管理セクションを再確認し、「検温」に相当する列を特定して「シフトチェック」に変更する。

#### 2. 手配表：検温併用削除（未対応）
- **場所**: `/src/app/tehai/page.tsx` 全体
- **期待**: 「検温」と「シフトチェック」を併用表示している箇所を削除し、シフトチェックのみにする
- **実際**: 上記と同様。対象UIが特定できない。
- **対応方針**: 要望の元となったCSVの文脈を確認し、どの列/セクションが「検温併用」を指すかを特定してから実装する。

### INFO（軽微）

#### 3. 手配表：文字・幅の最適化（部分対応）
- **場所**: `/src/app/tehai/page.tsx`
- **内容**: テキストサイズや列幅の細かい調整は入っているが、「最適化完了」の明確な基準がCSVに記載されていないため確認が難しい。UI上での表示確認を推奨。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## Quality Score
- 網羅率（修正済み/全要望）: 18/21 = **86%**
- 完全対応: 18件 ✅
- 部分対応: 1件 ⚠
- 未対応: 2件 ❌

## Verdict

**NEEDS REVISION** — 以下の2項目が未対応のため修正が必要:

1. **手配表 / 検温→シフトチェック表記変更**: `tehai/page.tsx` を精査し、「検温」に相当する列を特定してラベルを「シフトチェック」に変更する。
2. **手配表 / 検温併用削除**: 上記と連動して、「検温」と「シフトチェック」が併用されているUIを削除しシフトチェックのみにする。
